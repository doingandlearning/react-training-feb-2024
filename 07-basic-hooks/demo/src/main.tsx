import React from 'react'
import ReactDOM from 'react-dom/client'
import MemoApp from './MemoApp.tsx'
// import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  // <React.StrictMode>
  <MemoApp />
  // </React.StrictMode>,
)
